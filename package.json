{
  "name": "callcrew",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "deploy": "npm-do push-dir --dir=src --branch=gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jn",
  "license": "ISC",
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
